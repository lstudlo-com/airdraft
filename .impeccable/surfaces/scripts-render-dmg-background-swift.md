---
version: 1
slug: "scripts-render-dmg-background-swift"
primary_target: "scripts/render-dmg-background.swift"
related_targets: ["scripts/build-dmg.py","scripts/dmg-layout.json"]
---

# Finder installer

Mode: Operate. First-time users need to copy Airdraft into Applications and open
that installed copy. Preserve the familiar two real Finder icons and native drag
behavior. The user's Superwhisper reference establishes the immersive two-icon
composition; Airdraft's existing silver capsule and outlined wordmark own the art.

The capsule surrounds the install action. A short welcome introduces it, an arrow
connects source to destination, and two lines explain installation and first open.
Generate artwork from code at 1x and 2x. Allow Finder path/status bars to remain
visible without clipping instructions. Verify the mounted image, not only its
background, and preserve the signed app bundle unchanged.

Local Finder review passed. Release publication is outside this task.
